# Public Convex client URL, shown by `npx convex dev`.
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Set OWNER_EMAIL only in the Convex dashboard/environment, never in Render.
# npx convex env set OWNER_EMAIL you@example.com
